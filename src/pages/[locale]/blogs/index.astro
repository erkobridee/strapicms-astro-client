---
import Layout from '~/layouts/main.astro';
import Link from '~/components/Link.astro';
import RenderTheCode from '~/components/RenderTheCode.astro';

import { getEntriesSorted, mapByLocale } from '~/content/utils/blogs';

import { SITE_TITLE, locales } from '~/settings';

//----------------------------------------------------------------------------//

interface Blog {
  title: string;
  href: string;
}

export async function getStaticPaths() {
  const entries = await getEntriesSorted();

  const entriesByLocale = mapByLocale(entries);

  return locales.map((locale) => {
    const content = entriesByLocale.get(locale) || [];

    const contentLength = content.length;
    const contentAsCode = JSON.stringify(content, null, 2);

    const title = `Blogs | ${locale} | ${SITE_TITLE}`;

    const blogs = content.map(({ data }) => {
      const { title, slug } = data;

      return { title, href: `/${locale}/blogs/${slug}` } as Blog;
    });

    return {
      params: { locale },
      props: { contentLength, contentAsCode, title, blogs }
    };
  });
}

//----------------------------------------------------------------------------//

interface Props {
  contentLength: number;
  contentAsCode: string;

  title: string;

  blogs: Blog[];
}

const { contentLength, contentAsCode: code, title, blogs }: Props = Astro.props;
---

<Layout
  content={{
    title
  }}
>
  <div class="flex h-screen flex-col items-center gap-4 p-4">
    <h2>{title}</h2>

    <div class="w-full border-b border-gray-300"></div>

    <div class="flex gap-2">
      <Link href="/">Home</Link>

      <span>/</span>

      <span>Blogs</span>
    </div>

    <ul class="list-disc">
      {
        blogs.map(({ title, href }) => (
          <li>
            <Link {href}>{title}</Link>
          </li>
        ))
      }
    </ul>

    <div class="w-full border-b border-gray-300"></div>

    <h3>
      Loaded content [ length: {contentLength} ]
    </h3>

    <RenderTheCode {code} />
  </div>
</Layout>
